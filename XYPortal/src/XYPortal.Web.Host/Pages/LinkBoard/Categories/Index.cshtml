@page
@using XYPortal.LinkBoard.Permissions
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Mvc.Localization
@using XYPortal.Localization
@model XYPortal.Web.Pages.LinkBoard.Categories.IndexModel
@inject IAuthorizationService AuthorizationService
@inject IHtmlLocalizer<XYPortalResource> L
@* ReSharper disable once Razor.SectionNotResolved *@
@section styles {
    <abp-style src="/Pages/LinkBoard/Categories/index.css" />
}
@* ReSharper disable once Razor.SectionNotResolved *@
@section scripts {
    <abp-script src="/Pages/LinkBoard/Categories/index.js" />
}

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-md-6">
                <h5 class="card-title">@L["Menu:LinkBoard:Categories"]</h5>
            </div>
            <div class="col-md-6 text-end">
                @if ((await AuthorizationService.AuthorizeAsync(User, LinkBoardPermissions.LinkCategoryCreate)).Succeeded)
                {
                    <button id="NewCategoryButton" class="btn btn-primary">
                        <i class="fa fa-plus"></i> @L["LinkBoard:NewCategory"]
                    </button>
                }
            </div>
        </div>
    </div>
    <div class="card-body">
        <table id="CategoriesTable" class="table table-striped"></table>
    </div>
</div>
